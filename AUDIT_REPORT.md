# Delta V2 — 完整代码与数据审计报告

> 2026-06-13 | 基于 v8 panel 的系统性审计，整合历史记忆

---

## 审计发现汇总

### 问题层级图

```
根因层                     机制层                    表现层
┌─────────────┐       ┌──────────────┐       ┌───────────────────┐
│ 3 agents    │──────→│ 49 unique H  │──────→│ H_resid 加控制后  │
│ 1-10 scale  │       │ values only  │       │ t: -2.85→-1.66    │
│ same model  │       │ resolution   │       │ (边际显著→不显著) │
└──────┬──────┘       └──────┬───────┘       └───────────────────┘
       │                     │
       │              ┌──────┴───────┐       ┌───────────────────┐
       │              │ H×D 无独立   │──────→│ proper spec       │
       │              │ 变异         │       │ t=+0.69           │
       │              └──────────────┘       └───────────────────┘
       │
┌──────┴──────┐       ┌──────────────┐       ┌───────────────────┐
│ Rating受训练│──────→│ 知名股更一致 │──────→│ 知名股 H=1.35     │
│ 数据影响    │       │ (familiarity)│       │ vs 非知名 1.42    │
│ (name recog)│       │              │       │ t=-4.73***        │
└─────────────┘       └──────────────┘       └───────────────────┘

┌─────────────┐       ┌──────────────┐       ┌───────────────────┐
│ bm_proxy =  │──────→│ 与mom共线    │──────→│ 加bm后H_resid     │
│ -past_12m   │       │ r=-0.693     │       │ 从t=-2.00*→-1.66  │
│ (不是真BM)  │       │ 过度控制     │       │ (伪死亡)           │
└─────────────┘       └──────────────┘       └───────────────────┘
```

---

## 详细审计结果

### 1. 信号分辨率问题（根因）

**3 agents × 1-10 整数评分 → 只有 49 个 unique H_smooth 值**

- 13,340 个观测，仅 180 个 unique (s,t,f) 三元组（理论 1000 种）
- 映射到 49 个 unique H_smooth 值
- 前 5 个 H 值覆盖 42.1% 的观测
- 单个最常见值覆盖 9.8%

**为什么这是根因**：
- 截面 FM 需要足够的 cross-sectional variation 来区分高低熵股票
- 49 个值意味着同一月内大量股票有相同 H 值
- 加控制变量后，剩余独立变异更少 → 信号自然减弱
- H×D 交互效应无独立变异 → proper spec 下 t=+0.69

**历史记忆对齐**：
- ✅ 已知：记忆中 "3 agent→5-6唯一值"（实际 49，但有效变异远小于此）
- ✅ 已知：冬生判断 "信号太弱"，提出三条路（增强agent/换问题/换数据）

### 2. H_smooth_resid 与控制变量

**递进 FM 结果（关键数字）**：

| 控制变量组合 | H_smooth_resid t | 显著性 |
|---|---|---|
| 无控制 | -2.85 | *** |
| + rel_size | -2.73 | *** |
| + mom_6m_past | -2.30 | ** |
| + rel_size + mom + vol | -2.00 | * |
| + rel_size + mom + vol + bm_proxy | -1.66 | (p=0.10) |

**关键发现：bm_proxy_v2 是伪控制变量**

- `bm_proxy_v2 = -past_12m_return`，这不是真正的 Book-to-Market
- 与 mom_6m_past 相关系数 r = -0.693（高度共线）
- 加入 bm_proxy 等于加入第二个动量变量 → 过度控制
- H_smooth_resid 与 bm_proxy 相关 r = +0.313，但通过 mom 间接相关
- **H_resid 在 size+mom+vol 下仍然显著 (t=-2.00*)**，只有加 bm 才不显著

**解决方案**：获取真实 BM 数据（从 WRDS/Compustat），替换 bm_proxy

### 3. Horizon 效应 — 真正的亮点

| Horizon | 无控制 t | +size+mom+vol (无bm) t | Hodrick SE t |
|---|---|---|---|
| 1Q | -2.85*** | -2.00** | -3.02 |
| Cum2Q | -3.94*** | -3.36*** | -4.00 |
| Cum3Q | -3.97*** | -4.11*** | -4.96 |

**带控制的 Horizon 效应仍然显著**，而且 Hodrick SE 确认不是重叠伪影。
FM beta 自相关接近零（lag-1 = -0.022），重叠不严重。

这是 Delta 论文最可靠的发现。

### 4. Familiarity Bias

**不是通过 market cap，而是通过 name recognition**

- Size group（大/中/小）：H_smooth 无显著差异 (t=-0.53)
- 但特定知名股（AAPL, MSFT, GOOGL 等 20 只）的 H 值显著更低 (t=-4.73***)
- 最低熵 Top 10 全是 NVDA, AVGO, META, MSFT, AAPL 等

**路径**：知名公司 → LLM 训练数据更丰富 → 评分更自信（一致）→ 熵更低

**需要做的**：
1. 匿名化测试（去掉公司名，只保留数值特征）→ 最直接的检验
2. Familiarity proxy 回归（Wikipedia page length / analyst coverage）

### 5. H×D 交互效应

**论文声称 t=3.38*** 是误导性的**：
- 那是单独回归 H×D → Y（不含 H 和 D 主效应）
- 正确交互模型 Y ~ H + D + H×D：t = +0.69 (p=0.49)
- 根因：d_post 与 JS_correct 相关 r=0.948，H×D 几乎无独立变异

**结论**：H×D 交互效应不能作为独立发现。它是 49-value 问题的后果。

### 6. 三个 Agent 同模型问题

**记忆中已知的所有问题**：
- 语义漂移不是真正分歧
- 与 Miller(1977) 理论前提矛盾
- 需要换不同模型或不同信息架构

---

## 与评审四问题的映射

| 评审问题 | 审计发现 | 严重程度 | 解决路径 |
|---|---|---|---|
| Q1: 同模型=语义漂移 | ✅ 确认，且49值问题加剧 | 🔴 根因级 | 换三模型，增加agent数量 |
| Q2: 正交化不稳定 | H_resid 在合理控制下 t≈-2.0 | 🟡 可辩护 | 四象限+获取真BM数据 |
| Q3: look-ahead通过familiarity | 知名股更一致(t=-4.73)，但不是size通道 | 🟡 需要检验 | 匿名化测试 |
| Q4: 时序重叠SE低估 | FM beta自相关≈0，Hodrick确认 | 🟢 已解决 | 并排报告即可 |

---

## 论文真实状态评估

**可以声称的**：
1. H_smooth_resid 在合理控制(size+mom+vol)下预测回报 (t=-2.00*)
2. 效应在更长horizon增强 (Cum3Q t=-4.11*** Hodrick确认)
3. Familiarity bias 存在但不是 size 通道
4. 三模型分歧 > 单模型分歧（待验证）

**不能声称的**：
1. H×D 交互效应（proper spec 不显著）
2. 独立于所有已知因子的定价能力（bm_proxy 问题待解决）
3. "Entropy Premium"作为新的资产定价因子

**需要补做的关键实验**：
1. 匿名化评分（测 familiarity bias）
2. 获取真实 BM 数据（替换 bm_proxy）
3. 三模型评分（解决 Q1 + 增加 H 值分辨率）
4. 7-10 agent 版本（大幅增加熵分辨率）
